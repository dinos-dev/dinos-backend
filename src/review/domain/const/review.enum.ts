/**
 * 리뷰 단계 enum
 * @param BEFORE_ENTRY 입장전
 * @param ENTRY 입장
 * @param ORDER 주문
 * @param MEAL 식사
 * @param WRAP_UP 마무리
 */
export enum ReviewStep {
  BEFORE_ENTRY = 'BEFORE_ENTRY',
  ENTRY = 'ENTRY',
  ORDER = 'ORDER',
  MEAL = 'MEAL',
  WRAP_UP = 'WRAP_UP',
}
